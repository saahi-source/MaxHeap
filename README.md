# MaxHeap

Files

WordFreq.java – defines the WordFreq class with word, frequency, and a toString() method.

HeapDemo.java – main program that:

Creates an array of WordFreq objects

Implements maxHeapify, swap, and buildMaxHeap

Prints the array before and after heapification

Git Version History

Step 1 – Created WordFreq.java

Step 2 – Initialized array in HeapDemo.java

Step 3 – Added heap methods (maxHeapify, swap, buildMaxHeap)

Step 4 – Added printing before and after building max-heap

Final – Added toString() to WordFreq for readable output
